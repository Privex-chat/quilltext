{
  "name": "quilltext",
  "version": "2.0.0-beta.1",
  "description": "Quilltext - turn typed text into convincing handwritten pages. Free, open-source, no signup.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "jspdf": "^2.5.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  }
}
